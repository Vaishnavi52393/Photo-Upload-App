apiVersion: v1
kind: Service
metadata:
  name: photo-app-svc
spec:
  type: NodePort
  selector:
    app: photo-app
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080   # reachable at http://<any-node-ip>:30080
